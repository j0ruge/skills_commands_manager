{
  "name": "ansible-docker-backup-restore",
  "description": "Back up and restore a Linux server's Docker services with Ansible — volume tars, pg_dump/mysqldump, snapshot-guarded overwrite, and proof the nightly backup still runs. Catches restores that look green but are broken: 200s serving the wrong page, auth-plugin drift in restored datadirs, silently dead backups. Proves a dump by its contents (gzip -t, COPY blocks, a known record) instead of its size, and distrusts a backup container's healthcheck. Triggers — ansible, playbook, backup, restore, disaster recovery, volume snapshot, mysqldump, retention, dump integrity, backup healthcheck lies.",
  "author": {
    "name": "Chewiesoft"
  },
  "version": "1.3.0",
  "category": "development",
  "keywords": ["ansible", "playbook", "inventory", "host-vars", "ansible-vault", "backup", "restore", "disaster-recovery", "volume-snapshot", "docker-volume", "named-volume-prefix", "compose-project-name", "pg_dump", "mysqldump", "sql-dump-guard", "retention", "backup-freshness", "silent-failure", "ignore-errors-no-log", "tar-rc1-file-changed", "backup-exclusions", "anti-overwrite-guard", "force-restore", "network-alias-collision", "acme-state", "vhost-orphan-directive", "reverse-proxy-tls", "bind-mount-escapes-volume-ls", "backup-push-least-privilege", "caching-sha2-password", "auth-plugin-drift", "verify-body-contains", "lineinfile-silent-append", "secondary-error-masking", "dump-integrity", "gzip-t", "schema-only-dump", "copy-blocks", "backup-healthcheck-lies", "postgres-backup-local", "absence-of-signal", "prove-the-sensor"],
  "platforms": ["claude-code", "cursor"],
  "skills": "./skills"
}
