{
  "name": "dev-script",
  "description": "Generates a one-shot dev.sh / dev.ps1 launcher for any project — detects the stack (compose, monorepo, frontend, backend, IdP, mkcert), produces a single script that brings up the whole local infrastructure with colored prefixes per service, healthchecks, two-strategy port handling (find-next-free discovery with peer-coordination env vars for foreign-owned ports, kill-and-reclaim with pgrep fallback for own orphans), trap cleanup, idempotent re-runs, and (when applicable) HTTPS-on-LAN via mkcert + Caddy reverse proxy. Cross-platform: bash for Linux/macOS, PowerShell for Windows. Also guards Windows↔WSL migration traps — CRLF-corrupted .env reads, node_modules built for another platform (native-binding crashes), and the yarn↔cmdtest name collision (Corepack-aware package-manager resolution). Encodes the gotchas painfully learned in JRC projects so each new repo doesn't re-discover them.",
  "author": {
    "name": "Chewiesoft"
  },
  "version": "0.5.0",
  "category": "development",
  "keywords": [
    "dev-script",
    "dev.sh",
    "dev.ps1",
    "bootstrap",
    "local-development",
    "docker-compose",
    "mkcert",
    "caddy",
    "https-lan",
    "monorepo",
    "scaffold",
    "powershell",
    "bash",
    "playwright",
    "kill-port",
    "pgrep",
    "sanity-check",
    "runtime-drift",
    "tsx-watch",
    "port-discovery",
    "find-available-port",
    "strict-port",
    "peer-coordination",
    "crlf",
    "line-endings",
    "native-binding",
    "corepack",
    "cmdtest"
  ],
  "platforms": ["claude-code", "cursor"],
  "skills": "./skills"
}
